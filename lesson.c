#include <stdio.h>

int main(void) 
{
    int var;
    int total = 1024; //инициализация переменной
    var = 100; //операция присваивания двнных

    // int s, v; //объявление нескольких переменных
    // s = v = 200; 

    return 0;
}